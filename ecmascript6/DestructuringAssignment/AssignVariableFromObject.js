const { name: userName, age: userAge } = user;

//Atribuição de variáveis de objetos aglomerados
const { johnDoe: { age, email }} = user3; 

const { johnDoe: {age: user2Age, email: user2Email}} = user2;
